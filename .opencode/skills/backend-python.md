# Skill: Python & API Development
- **Execution**: Prefer `ruff` for all linting and auto-fixing.
- **Testing**: Use `pytest`. Always run with `--lf --tb=short` to minimize output noise.
- **Type Safety**: Ensure Type Hints are used in all new function definitions.
- **Log Hygiene**: Use structured logging. When debugging, look for `exc_info=True`.