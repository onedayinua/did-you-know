# Skill: React & UI Development
- **Linting**: Run `npx eslint --fix` after component changes.
- **Styling**: Maintain consistency with existing CSS/Tailwind patterns.
- **Hooks**: Prefer Functional Components. Ensure `useEffect` dependencies are exhaustive.
- **Verification**: Use `npm test` for logic verification.